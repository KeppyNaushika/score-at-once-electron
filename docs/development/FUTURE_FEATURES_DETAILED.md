# 将来実装予定機能の詳細仕様

このファイルは、現在未使用だが将来の機能実装で使用予定の変数・関数について、その詳細な実装計画と仕様を記録します。

## 🎯 採点機能の高度化 (07-score-at-once)

### 📊 統計・分析機能

#### `getScoringStatus` - 採点状況の詳細分析
**目的**: 採点の進捗と品質を詳細に分析・可視化する

**実装予定機能**:
- **採点完了率の詳細分析**: 問題別、生徒別、教員別の完了状況
- **採点品質メトリクス**: 教員間の採点傾向の差異分析
- **採点速度分析**: 問題別の平均採点時間、効率性指標
- **未採点箇所の優先度付け**: 重要度に基づく採点推奨順序

**返り値の想定構造**:
```typescript
interface ScoringStatusDetail {
  totalProgress: number // 全体進捗率
  questionProgress: QuestionProgressDetail[] // 問題別進捗
  teacherComparison: TeacherScoringComparison[] // 教員間比較
  recommendedActions: RecommendedAction[] // 推奨アクション
  qualityMetrics: QualityMetrics // 品質指標
}
```

#### `getAllGridAnswerData`, `getMasterAnswerData` - データ分析基盤
**目的**: 採点データの包括的な取得と分析処理

**`getAllGridAnswerData`の役割**:
- グリッド表示用の全答案データを効率的に取得
- フィルタリング条件に基づくデータの事前処理
- 大量データの遅延読み込み対応
- キャッシュ機能による高速表示

**`getMasterAnswerData`の役割**:
- 模範解答との比較分析データの生成
- 自動採点支援のための特徴量抽出
- 類似解答のグループ化
- 採点基準の一貫性チェック

### 🔍 フィルタリング・表示機能

#### `setFilterSettings` - 高度なフィルタリング設定
**目的**: 複雑な条件による答案絞り込み機能

**実装予定機能**:
- **複合条件フィルタ**: AND/OR条件を組み合わせた複雑な絞り込み
- **時系列フィルタ**: 採点日時、更新日時による絞り込み
- **品質フィルタ**: 採点品質スコアによる絞り込み
- **カスタムフィルタ**: ユーザー定義の条件設定
- **フィルタプリセット**: よく使用する条件の保存・復元

**設定項目例**:
```typescript
interface AdvancedFilterSettings {
  scoreRange: { min: number; max: number }
  scoringStatus: ScoringStatus[]
  dateRange: { start: Date; end: Date }
  teacherIds: string[]
  customConditions: CustomCondition[]
  presets: FilterPreset[]
}
```

#### `recentlyScoredAnswers` - 採点履歴管理
**目的**: 最近採点した答案の履歴表示と効率的なレビュー

**実装予定機能**:
- **時系列履歴表示**: 採点順序に基づく答案一覧
- **クイックレビュー**: 最近の採点結果の確認・修正
- **採点パターン分析**: 個人の採点傾向の可視化
- **undo/redo機能**: 採点の取り消し・やり直し
- **採点コメント履歴**: 過去のコメントの参照・再利用

### 🖼️ 画像表示・操作機能

#### `imageRef`, `canvasRef` - 高度な画像操作
**目的**: より精密で効率的な画像表示・編集機能

**`imageRef`の活用予定**:
- **画像の詳細情報取得**: 実際のピクセルサイズ、メタデータ
- **高精度座標計算**: 画像内の正確な位置特定
- **画像品質の動的調整**: 表示サイズに応じた解像度最適化
- **画像フィルタ適用**: コントラスト調整、ノイズ除去

**`canvasRef`の活用予定**:
- **リアルタイム描画**: 採点マークの即座反映
- **レイヤー管理**: 元画像と採点情報の分離表示
- **図形描画ツール**: 直線、矢印、図形による注釈機能
- **手書き入力**: タブレット対応の手書きコメント機能

#### `imageSize` - 画像サイズ情報活用
**目的**: 画像サイズに基づく最適化と表示制御

**実装予定機能**:
- **アスペクト比の自動調整**: 画面サイズに応じた最適表示
- **解像度別表示切り替え**: 低解像度プレビューと高解像度詳細表示
- **画像サイズ情報の表示**: ファイル情報として表示
- **印刷時のサイズ調整**: 用紙サイズに合わせた自動調整

### 🎮 キーボード・ナビゲーション機能

#### `currentStudentIndex`, `currentQuestionIndex` - 詳細ナビゲーション
**目的**: キーボードによる高効率ナビゲーション

**実装予定機能**:
- **連続採点モード**: 自動的な次の答案への移動
- **採点状況に応じたスキップ**: 未採点のみを対象とした移動
- **ブックマーク機能**: 特定の答案への瞬間移動
- **採点履歴に基づく移動**: 前回中断した箇所からの再開

#### `answerSheetsLength`, `questionRegionsLength` - 進捗表示
**目的**: より詳細で直感的な進捗情報の提供

**実装予定機能**:
- **進捗バーの詳細表示**: 全体・問題別・生徒別の進捗
- **残り時間予測**: 採点速度に基づく完了時間予測
- **効率性指標**: 時間あたりの採点数、品質スコア
- **目標設定機能**: 日次・週次の採点目標と達成状況

### 🏷️ ラベル・分類機能

#### `currentQuestion` - 問題情報表示
**目的**: 現在採点中の問題に関する詳細情報の表示

**実装予定機能**:
- **問題文の表示**: 採点画面上での問題文確認
- **採点基準の表示**: 配点、部分点の基準表示
- **過去の採点例**: 同一問題の他の採点例参照
- **難易度指標**: 問題の難易度と正答率情報

#### `currentProgress` - 詳細進捗情報
**目的**: 現在の採点進捗の詳細な可視化

**実装予定機能**:
- **リアルタイム進捗更新**: 採点と同時の進捗反映
- **複数軸での進捗表示**: 時間軸、品質軸での進度表示
- **チーム進捗比較**: 他の採点者との進捗比較
- **アラート機能**: 遅延やエラーの通知

### 🎨 表示・UI機能

#### `lastUpdated` - 更新時刻表示
**目的**: データの新しさと同期状況の明確化

**実装予定機能**:
- **リアルタイム更新表示**: 「○秒前に更新」形式での表示
- **同期状況の表示**: データベースとの同期ステータス
- **更新履歴**: 変更履歴の詳細表示
- **競合検知**: 他の採点者による同時編集の検知

#### `getProgressColor` - 進捗状況の視覚化
**目的**: 進捗状況の直感的な色分け表示

**実装予定機能**:
- **段階的色分け**: 進捗率に応じたグラデーション表示
- **状態別色分け**: 未着手・進行中・完了・エラーの色分け
- **アクセシビリティ対応**: 色覚異常にも配慮した表示
- **カスタム色設定**: ユーザー好みの色設定機能

## 📤 出力機能の詳細化 (08-export)

### 📊 出力進捗の詳細表示

#### `totalSteps`, `currentStepIndex` - 詳細な進捗ステップ表示
**目的**: 出力処理の詳細な進捗状況の可視化

**実装予定機能**:
- **ステップ別進捗表示**: 「データ取得中」「PDF生成中」等の詳細ステップ
- **推定完了時間**: 各ステップの推定所要時間表示
- **並列処理の可視化**: 複数の処理が同時実行される場合の表示
- **エラー時の詳細情報**: どのステップでエラーが発生したかの特定

**ステップ構成例**:
```typescript
interface ExportStep {
  id: string
  name: string
  description: string
  estimatedDuration: number
  status: 'pending' | 'running' | 'completed' | 'error'
  progress: number // 0-100
  subSteps?: ExportStep[]
}
```

**実装予定のステップ**:
1. **データ検証ステップ**: 出力対象データの整合性チェック
2. **データ取得ステップ**: データベースからのデータ取得
3. **変換処理ステップ**: データの出力形式への変換
4. **ファイル生成ステップ**: 実際のファイル作成
5. **品質チェックステップ**: 生成されたファイルの検証
6. **保存・配置ステップ**: 最終的なファイル配置

## 🖼️ テンプレート編集の高度化 (02-template)

### 🎨 レンダリング最適化機能

#### `containerReady`, `forceUpdate` - レンダリング制御
**目的**: より滑らかで応答性の高いテンプレート編集体験

**`containerReady`の活用予定**:
- **段階的レンダリング**: DOM準備完了後の安全な描画開始
- **レイアウト計算の最適化**: コンテナサイズ確定後の精密計算
- **初期化タイミングの制御**: 適切なタイミングでの機能有効化
- **エラー回避**: DOM未準備時のアクセスエラー防止

**`forceUpdate`の活用予定**:
- **ウィンドウリサイズ対応**: 画面サイズ変更時の強制再描画
- **データ更新時の同期**: 外部データ変更時の即座反映
- **パフォーマンス最適化**: 必要時のみの選択的更新
- **デバッグ支援**: 開発時の手動更新機能

### 🔧 画像処理機能

#### `getImageBounds` - 高精度境界取得
**目的**: より正確で柔軟な画像境界の計算

**実装予定機能**:
- **動的境界計算**: ズーム・パン状態に応じた境界計算
- **複数解像度対応**: 異なる解像度画像での統一的な境界処理
- **回転・変形対応**: 画像変形時の境界計算
- **境界外領域の処理**: 画像外へのレイアウト配置対応

**計算対象**:
```typescript
interface ImageBounds {
  visual: Rectangle // 実際の表示領域
  logical: Rectangle // 論理的な画像領域
  interactive: Rectangle // 操作可能領域
  scrollable: Rectangle // スクロール可能領域
}
```

## 📊 問題グループ管理の機能拡張 (04-question-group)

### 💾 保存機能の実装

#### `Save` アイコン、`handleSave` - 手動保存機能
**目的**: より柔軟で安全な保存オプションの提供

**実装予定機能**:
- **手動保存**: 自動保存に加えた明示的な保存操作
- **保存前確認**: 変更内容の確認ダイアログ
- **バージョン管理**: 保存時点での版管理
- **保存失敗時の復旧**: ネットワークエラー等への対応

#### `hasChanges` - 変更検知機能
**目的**: 未保存の変更に対する適切な警告と保護

**実装予定機能**:
- **リアルタイム変更検知**: 編集操作の即座検知
- **離脱時警告**: 未保存変更がある場合の離脱防止
- **変更内容の可視化**: 何が変更されたかの詳細表示
- **自動保存との連携**: 変更内容に応じた自動保存頻度調整

### 🖱️ ドラッグ操作機能

#### `dragStart` - ドラッグ選択機能
**目的**: より直感的で効率的な複数選択操作

**実装予定機能**:
- **矩形選択**: ドラッグによる範囲選択
- **連続選択**: Shiftキーとの組み合わせ選択
- **除外選択**: Ctrlキーとの組み合わせ除外
- **選択状態の可視化**: 選択範囲のハイライト表示

## 📋 答案管理機能の拡張 (06-answer-sheets)

### 🔧 セル操作機能

#### `onToggleCell` - セル単位制御
**目的**: より細かい粒度での答案管理

**実装予定機能**:
- **個別セル有効化/無効化**: 特定位置の答案の有効性制御
- **一括操作**: 行・列単位での有効化/無効化
- **条件付き操作**: 特定条件下でのセル状態変更
- **操作履歴**: セル操作の取り消し・やり直し

#### `studentId`, `pageNumber` - セル情報活用
**目的**: セル固有情報の詳細表示と操作

**実装予定機能**:
- **ツールチップ表示**: セル上でのマウスオーバー情報
- **コンテキストメニュー**: 右クリックによる操作メニュー
- **詳細情報パネル**: セル選択時の詳細情報表示
- **関連情報表示**: 同一生徒・同一ページの他セル情報

### 🎨 視覚化機能

#### `getFileColor` - ファイル状態の色分け
**目的**: ファイル状態の直感的な視覚化

**実装予定機能**:
- **状態別色分け**: 未処理・処理中・完了・エラーの色分け
- **品質別色分け**: ファイル品質に応じた色調整
- **優先度別色分け**: 処理優先度に応じた色表示
- **カスタム色設定**: ユーザー定義の色設定

#### `position` - 位置情報活用
**目的**: セル位置に基づく機能拡張

**実装予定機能**:
- **座標ベース操作**: 位置情報を活用した操作
- **近隣セル参照**: 隣接セルとの関係性表示
- **位置ベースフィルタ**: 特定位置のセルのみ表示
- **レイアウト最適化**: 位置情報に基づく表示最適化

## 🔄 状態管理機能

#### `setImageLoadStates` - 画像読み込み状態管理
**目的**: 大量画像の効率的な読み込み管理

**実装予定機能**:
- **遅延読み込み**: 必要時のみの画像読み込み
- **読み込み優先度制御**: 表示順序に応じた読み込み順序
- **キャッシュ管理**: 読み込み済み画像のメモリ管理
- **エラー処理**: 読み込み失敗時の代替表示

## 💡 実装時の技術的考慮事項

### パフォーマンス最適化
- **遅延読み込み**: 大量データの段階的読み込み
- **仮想化**: 表示領域外データの効率的管理
- **キャッシュ戦略**: 頻繁アクセスデータの高速化
- **メモリ管理**: 不要データの適切な解放

### ユーザビリティ
- **直感的操作**: 学習コストの低い操作方法
- **アクセシビリティ**: 障害者対応・多様な入力デバイス対応
- **レスポンシブ**: 様々な画面サイズへの対応
- **国際化**: 多言語対応の準備

### 拡張性
- **プラグイン構造**: 機能の追加・削除が容易な設計
- **API設計**: 外部システムとの連携可能性
- **設定可能性**: ユーザー・組織ごとのカスタマイズ
- **データ移行**: 既存データとの互換性維持

### セキュリティ
- **権限管理**: 機能ごとのアクセス制御
- **データ保護**: 機密情報の適切な保護
- **監査ログ**: 操作履歴の記録・追跡
- **入力検証**: 不正データの防止

---

## 📝 開発優先度

### 高優先度 (すぐに実装予定)
1. `getScoringStatus` - 採点状況分析
2. `setFilterSettings` - 高度フィルタリング
3. `totalSteps`, `currentStepIndex` - 出力進捗詳細

### 中優先度 (中期実装予定)
1. 画像操作機能 (`imageRef`, `canvasRef`)
2. セル操作機能 (`onToggleCell`)
3. 保存機能 (`handleSave`, `hasChanges`)

### 低優先度 (長期実装予定)
1. ドラッグ選択機能 (`dragStart`)
2. 詳細分析機能 (`getAllGridAnswerData`)
3. レンダリング最適化 (`containerReady`, `forceUpdate`)

## 🔄 更新履歴

- 2025年7月24日: 詳細仕様の初回作成
- 各機能の実装計画と技術仕様を網羅的に記録