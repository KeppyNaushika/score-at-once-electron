# これからの課題

**0.18.1-beta（2026-09-09 時点）。** ここから先にやることを置く。

**この文書には、これからのことだけを書く。** 済んだら消す。何をなぜ直したかは
issue とコミットに残るので、ここへ記録を積み上げない。**段階0〜70 は済んでおり**、
番号だけがコードのコメントに残っているので、末尾に「段階番号の索引」を置いた。

段階の番号は通し番号を引き継ぐ。**番号は識別子であって順序ではない。**

---

## 一覧

| 順  | #              | 課題                                         | 何のためか                                         | 重さ |
| --- | -------------- | -------------------------------------------- | -------------------------------------------------- | ---- |
| 1   | **段階71**     | 撮影の spec を書き直す                       | 使い方ガイドの図が、変わった画面に追いついていない | 中   |
| 2   | **段階72**     | 開発時のデータ置き場をリポジトリの外へ       | 配布物への混入の根を断つ                           | 小   |
| 3   | **段階73**     | CI の依存の入れ直しをやめる                  | ロックとずれるとリリースのビルドが落ちる           | 小   |
| 4   | **段階74**     | 07 の再描画を測る                            | 採点の体感で残っている最後の要素                   | 小   |
| 5   | **段階44**     | 採点領域を複数の矩形にし、途中改段組を入れる | 別欄の途中過程まで含めて1つの設問として採点する    | 大   |
| 6   | **段階59**     | 評定を実体にし、基準に無い評価を見せる       | 「／」のような自動算出できない評定を扱う           | 大   |
| 7   | **段階60**     | アーカイブ4種を1つに統合する                 | 4つの取り込みが別々の規則で動いている              | 大   |
| —   | **将来の検討** | 畳みを取り消せるようにする                   | いまは実装しない（OWNER 判断）                     | —    |

**1〜4 はリリースの過程で見つかったもの**で、どれも独立して着手できる。
**5〜7 は前から積んであるもの**で、どれも大きい。

---

## 段階71 — 撮影の残りを片付ける

**土台は `c930d6f9` で直した。** `testDir` が設定ファイルの位置を起点に解決される
ために撮影が1本も走っておらず、出力に並んでいたのは3月の古い絵だった、というのが
根にあった問題で、その先の壊れ方は全部それに隠れていた。併せて次が片付いている。

- 専用ポート 3124・`reuseExistingServer: false`・`NEXT_DIST_DIR: ".next-screenshot"`
  へ移した（`npm run dev` を開いたまま撮ると**そちらのコードに対して**撮れる罠）
- `catch` は29箇所から**2箇所**へ。ログインの失敗を握り潰さなくなった
- seed を `seedCli.ts` / `setup-data.ts` / `helpers/` へ出し、撮影の spec と分けた
- 実運用の DB へ繋がりうる穴を2段構えで塞いだ（`helpers/syncGuard.ts` と
  `helpers/databasePathProbe.ts`）
- **8. 採点確定**と**監査ログ**の画面も撮るようになった

**リリースの成果物ではない。** 出力（`__tests__/screenshots/output/`）は `.gitignore`
済みで `src` からも参照されていない。使い方ガイドの図は画面の中の SVG とアニメで、
撮った画像は使っていない。だから急がない。

### 残っているもの

|                                              |                                                                                                                                                   |
| -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| `waitForTimeout` が57箇所                    | 条件ではなく秒数で待っている。遅いうえ、機械が重い日は途中の絵が撮れる。**画面が増えて件数も増えた**                                              |
| **なぜ 1440 なのかが書かれていない**         | 一覧の操作は幅で「…」へ畳まれるので、**この数字が図に写るものを決めている**。残さないと、誰かが軽い気持ちで変えて図から操作が消える               |
| 1倍で撮っている                              | 資料に貼ると粗い。`--force-device-scale-factor=2` で2倍にできる                                                                                   |
| ルートの `playwright.config.ts` が死んでいる | `testDir: "./tests"` を指すがそのディレクトリは無い。素で `npx playwright test` を叩くと**0件で緑になる**。実体は `__tests__/` 配下の2つの config |

画面サイズ `1440×900` 自体は明示されており効いている（出力の PNG で確認済み）。

---

## 段階72 — 開発時のデータ置き場をリポジトリの外へ

`electron-src/lib/dataManager.ts:36`

```js
} else {
  // 開発環境の場合
  const rootPath = process.cwd()   // ← リポジトリの根
}
// → getDataDirectory() = <リポジトリ>/data
```

**開発と運用が同じ置き場を見ている。** 0.17.0 の直前に、配布物へその中身が丸ごと
入っていたのが見つかった（0.17.0 直前のリリース作業）。
`forge.config.js` の除外を許可制へ裏返し、できあがりを見て止める関門も置いたので
いまは塞がっているが、**根はここ**である。

置き場を `~/Library/Application Support/…` などへ移せば、

- 配布物への混入が**構造的に**起こらなくなる（許可制の除外は二重の歯止めとして残る）
- `.gitignore` の `data/` も要らなくなる
- 文書に「開発時のデータ置き場はリポジトリ直下」と書かなくて済む

**移す手順が要る。** 既にそこで運用している人（＝自分）のデータを、新しい置き場へ
運ぶ道を用意しないと、次の起動で空のDBが生まれる。

---

## 段階73 — CI の依存の入れ直しをやめる

`.github/workflows/build-and-release.yml` が、`npm install` のあとに Tailwind と
lightningcss を **uninstall してから版を指定して入れ直している**。

0.17.0 のビルドはこれで落ちた。

```
TypeError: designSystem.canonicalizeCandidates is not a function
    at scripts/canonicalizeTailwindClasses.mjs:285
```

`package-lock.json` は 4.3.3、CI は 4.1.7 を入れており、`canonicalizeCandidates` は
4.3 系の内部 API なので **CI だけで落ちる**。版を揃えて通した（`f93f1528`）が、
**仕組みが残っている限り、次に版が動けばまた落ちる。**

`npm ci` にすればロックのとおりに入るので、ずれが構造的に起きなくなる。ただし
**入れ直しは 2025-07-26 の「compatibility」という一行のコミットで入ったもので、
当時何を直したのかの記録が無い**。ネイティブモジュール（lightningcss / oxide）の
解決を回避するためだったと思われるので、外して通るかを確かめてから。

---

## 段階74 — 07 の再描画を測る

段階70 で1打鍵の取り直しを「書いた設問1つぶん」まで狭めた。OWNER の体感は
**「選択は素早く移り、その 0.2 秒後くらいに色が付く」**で、許容されている。

残っているのは **React の再描画**で、そこだけ測っていない。DB・IPC・進捗の計算は
測ってあり、いずれも小さい。0.2 秒の大半は描画側にあると見ている。

**急がない。** 触るなら、まず測ってから。

---

## 段階44 — 採点領域を複数の矩形にし、途中改段組を入れる（後回し）

**OWNER 決定・2026-08-21。** 単独では着手しない。**複数領域の採点を機能として設計し、
途中改段組と合わせて実装する。**

### なぜ1つの段階なのか

途中改段組は原稿用紙を段またぎで割る。**割った瞬間に、1つの採点領域が紙の上で2つの矩形に
なる。** 採点・注釈・出力の全経路が「1領域＝1矩形」を前提にしているので、分割だけを先に
入れると採点が壊れる。だから片方だけでは入れられない。

### 複数領域の採点（検討事項・OWNER 提起）

**別欄に書かせた途中過程を、設問と一緒に採点したい。** 解答欄と、離れた場所にある
途中過程の欄が、**1つの設問の採点対象**になる。段またぎで割れた原稿用紙は、この一般形の
特殊な場合にすぎない。

決めること:

- **一覧表示と詳細表示** — 採点画面で、複数の矩形をどう見せるか。全部を並べて一度に見せるか
  （一覧）、1つずつ大きく見せるか（詳細）、その行き来をどうするか
- **矩形の順序** — 何番目の矩形か、を何で決めるか（**序数を key にしない**。同定は id）
- **アノテーションの帰属** — 注釈はどの矩形に属するか。矩形をまたぐ注釈を許すか
- **切り出し画像** — いまは1領域＝1画像。複数矩形をどう1枚にするか（並べる／別々に持つ）
- **出力** — Excel・PDF・個人成績表で、複数矩形の設問をどう出すか
- **OMR** — マーク欄が複数矩形になる場合があるか

### 途中改段組（設計は確定・実装は上記と同時）

**分割の単位は「行」。**

- **原稿用紙** — マス目の行がそのまま分割の単位。20行なら切れ目の候補が20個ある
- **それ以外の解答欄** — 高さが2でも3でも**まるごと1行**。切れ目は無い

いまの実装は原稿用紙を「高さ `rows × 倍率` の1行」に潰している
（`gridBuilder.ts:186-191`）。通常のセルと同じ形にしてしまっているので切れ目が消えている。
ここを**行の集まりとして持てるようにする**のが土台になる。

いまの配置ループ（`computeMultiPageLayout.ts:689`）は、

```ts
if (colCurrentY[...] + spacingHeight + majorHeight > contentMaxY &&
    colCurrentY[...] > contentTop) {
```

第2条件のため、**段の先頭に置いた大問が入りきらない場合はそのまま溢れる**。次の段へも
次のページへも送られない。これが「大問内分割が未実装」の実体である。

### 強制改段組と、分離禁止の3スイッチ

**加算だけで済ませる。** `NextPlacement`（`"inline" | "break"`）の union を延ばすと既存の
値の意味に手が入るので、**別の列**を足す。

分離禁止は **ASB ごと**（`AsbDefinition` の列）の3スイッチで、**どの深さの行を切れ目として
認めるか**を決める。

| スイッチ               | オンのとき                                              |
| ---------------------- | ------------------------------------------------------- |
| 大問の途中で分離しない | 小問の境目の行も使わない＝大問はまるごと1つの段へ       |
| 小問の途中で分離しない | 枝問の境目の行も使わない                                |
| 枝問の途中で分離しない | **原稿用紙の行を使わない**＝原稿用紙はまるごと1つの段へ |

**既定値は未決**。3つともオンで始めれば既存の解答用紙の見た目が変わらない、という利点が
ある（分割は着手時に改めて決める）。

### 将来の案（いま混ぜない）

**段組セクション** — 大問より上の単位で段組みを変える。「ここからは1段」「ここからは2段」
という区切りを置く。段組みの設定が用紙全体で1つしか無いという制約を解くもので、
**別の軸**なので段階44 には混ぜない（OWNER 提起・2026-08-21）。

---

---

## 段階59 — 評定を実体にし、基準に無い評価を見せる（リリース後）

**OWNER との設計討議・2026-08-22。** 調査中（材料が揃い次第ここへ足す）。

### 出発点 —— 順序が逆になっている

> 結局さ、**成績というのが、あくまで評価の後にある**、という前提ですよね。まず評価があって、
> **それを何点として解釈するのか**、という思想があるから、評価が任意入力で、後付けで
> 点数変換を行っている。ここにちゃんと向き合わないと解決しない。（OWNER）

いまのコードは「**境界が評定を定義する**」という前提で書かれている（`ConstraintRulesEditor` の
`collectLabels` が境界からラベルを集める）。だが実際の思想は逆で、**評定が先にあり、境界は
そこへ至る一つの手段にすぎない**。だから結果表の任意入力（`resolveOverrideDirection` の
`"custom"`）が居場所を失い、「その他」扱いになっている。

**評定という実体が無いまま、それを使う側だけが4つある。** FK が張れず、文字列照合になり、
名前が `label` に濁り、任意入力が宙に浮く —— **全部同じ根**である。

| 役割                     | いまの表                                              |
| ------------------------ | ----------------------------------------------------- |
| **評定そのもの（語彙）** | **どこにも無い**                                      |
| 導く手段                 | `GradeItemBoundary`                                   |
| 直接下す                 | `GradeOverride`（**基準に無い値も打てる**）           |
| 後付けの解釈（何点か）   | `GradeConstraintLabelValue`                           |
| 混在を禁じる             | `GradeConstraintExclusionLabel`                       |
| 資料の評語               | `CourseworkLetterScale`（**同じ概念か別かは調査中**） |

### 決まった仕様（OWNER）

1. **評価は Excel から自由に貼れる。** プルダウンで選ばせる形にはしない
2. **全角→半角は、貼り付けた瞬間に確認する** —— 「全角文字が検知されました。半角文字で
   よろしいですか？」。設定として持たない（置き場所も、アーカイブに載せるかも問わずに済む。
   利用者はそれが効く瞬間にだけ判断する）
3. **大小文字は区別する。** `A` と `a` は別の評定（いまは `letterKey` の `.toUpperCase()` が
   畳んでいる。**その1行を落とす**）
4. **基準が1つ以上定められているとき、基準に定められていない評価のマスを赤くする**
5. **弾かない。そのまま保存する**

**typo に気づく場所は「基準を決めるとき」であって、評価するときではない**（OWNER）。だから
評価の画面に typo 防止の UI を持ち込まず、**赤いマスで気づかせる**。

### 正規化についての方針転換

当初は「語彙を先に宣言し、正規化で吸収して照合し、合わなければ拒否」と考えたが、**OWNER が
これを退けた**。正規化は「そう打ちたかったのだろう」という**推測**であり、このリポジトリが
他所で徹底している「黙って変換しない」に反する。

ただし**全角→半角は推測ではない**（表記の違い）ので残し、**スイッチで切れるようにする**。
**大小文字を畳むのは推測**なのでやめる。

**`normalizeInput` は撤去しない。** 撤去するのは `letterKey` の `.toUpperCase()` だけ。

### 資料側は既にこの形に近い

`CourseworkScoresContainer.tsx:46-64` に doc コメントごと在る。

> Excel から貼り付けた `１０` や `ａ` は間違いではなく表記の違いなので、弾かずに受け入れる。
> 文字評価の照合では**変換表のラベル側にも同じ正規化を通すこと**

**成績側だけがこの形になっていない**（境界から語彙を導き、上書きは素通し、正規化も照合も無い）。

### 決着（2026-08-22）—— 外部資料の側は実装済み `e2adfef9`

**話が2つ混ざっていた。** 貼り付けと自由入力の議論は**外部資料（Coursework）**の点数入力で、
成績（Grade）の評定とは別である。

**外部資料** —— 仕様どおり実装した。`A`/`a` を区別、全角は貼り付け1回につき一度だけ尋ねる
（設定は持たない）、変換表に無い評語も保存する、赤は変換表が1本以上あるときだけ、評価項目の
画面に件数つきで列挙する（変換表へ足す導線は置かない）。**計算で欠測になるのは仕様**なので
手当てしない。

**成績の上書きも自由でなければならない**（OWNER）。自動算出できない「**／**」を、校長判断で
評定として与えることがある。「基準のない自動算出成績は不要」は**自動算出の側**の話で、
上書きの側は逆である。外部資料と同じ形（そのまま保存・赤・列挙）にするのが筋。

**観点間制約は、基準外の評定を自動的に外してよい**（OWNER）。

> そもそも制約は**自動算出において**チェックするもの。校長判断については観点だけでなく評定も
> 一括して判断するもの。**できる範囲で検査すればよい。**

つまり**いまの挙動がそのまま仕様**である —— 基準外の観点は平均の母数から落ち、比較先が
基準外なら違反なしになる。**「黙って母数が縮む」を問題として扱わない。** 手を入れない。

**`GradeFrozenScore` の名前** —— 採点側が「確定」を `07-2-finalize` と呼ぶ計画なので、
`Finalize` 系へ揃えるかは別途。

### 実装で決めること（未定）

- **全角→半角スイッチの置き場所** —— 成績・資料ごと（アーカイブに載る）／アプリ全体の設定
  （載らない）／評価項目ごと。**アーカイブと一緒に持ち運ばれるかで意味が変わる**
- 語彙テーブルを作るなら、**一致しない生の値をどう保つか**。`overrideLabel`（生）＋
  `gradeLabelId`（解決済み・null 可）の2列が素直
- 赤くするのは基準の画面だけか、**評価を入れる画面でも**か
- **赤い評価がその先（換算・分布・出力・確定・アーカイブ）でどう扱われているか** —— 調査中。
  ここが次の設計の入口
- `CourseworkLetterScale` を同じ語彙へ寄せるか（**同じに見えて違う可能性**。資料は評語ごとに
  点数を持ち、成績の換算は制約ごとに数値を持つ）

### リリースには含めない

語彙の実体化・migration・4画面・アーカイブの版上げと変換器に波及し、**「基準に無い評定を
どう足すか」という UI 設計**も新たに要る。

**段階33 で決めた `@unique` の判断は、この設計が入れば作り直しになる**（`(constraintId, label)`
が `(constraintId, gradeLabelId)` ＝ uuid のみの複合 unique へ移る）。ただし**入るまでは今の
判断が正しい**ので無駄にはならない。

---

---

## 段階60 — アーカイブ4種を1つに統合する（**将来・リリース後**）

**OWNER 予定・2026-08-22。** いまアーカイブは**5種類**ある（数え直した。当初「4種」と
書いたのは `.students` を数えていなかったため）。

| 種             | 拡張子        | 現行版 | 変換器 |
| -------------- | ------------- | ------ | ------ |
| 試験           | `.score`      | 1.27.0 | 27本   |
| 試験外成績資料 | `.coursework` | 1.2.0  | 2本    |
| 成績           | `.grade`      | 1.16.0 | 9本    |
| 解答用紙       | `.asb`        | 1.5.0  | 5本    |
| 生徒           | `.students`   | 1.0.0  | 0本    |

**版は動く。** 現行版の出どころは `src/types/*Archive.types.ts` の `*_CURRENT_VERSION`
1箇所だけなので、この表と食い違ったらそちらが正しい。構造は
[import-export-architecture.md](./import-export-architecture.md) にある。

### なぜ統合するのか（推測ではなく、いま観測できている食い違い）

**同じことを5回別々に書いている。** 全種を実測して分かったことを、統合の材料として残す。
**取り込みの3択（`overwrite` / `merge` / `separate`）と `allowCreate` は既に揃えたので、
残っているのは下の4つ。**

- **3択が効かない種がある** —— `importValuePolicy` を通しているのは試験・資料・生徒の3種で、**成績と解答用紙は通っていない**。解答用紙は「常に新規作成」で照合の段そのものが無いので設計として一貫しているが、成績は3択を持ちながら値と時刻の倒し方が別実装のまま
- **同じ生徒・学級を、種ごとに別のコードで照合している** —— `merge/matchers/`（試験）と `coursework-archive/idRemapper.ts`（資料・成績が呼ぶ）と `student-archive/`（生徒）。`new Map(rows.map(...))` が黙って最後の1件を採る穴を**2箇所で同時に直す**羽目になったのは、この重複が原因
- **既定値の埋め方も種ごと** —— 試験は `archiveExtractor` の1か所へ寄せたが、`gradeArchiveImporter` には**同じ形の `for...of` が14か所**残っている
- **版の検出も揃っていない** —— 試験は manifest ＋ 形状フロア、成績は**形状のみ**（`manifest.version` が 1.6.0 固定で嘘をついていた歴史がある）

### 統合するとき、まとめて解けるもの

- **人が統合先を選ぶ導線**（段階61 で試験へ入れる）を1つにする
- **生徒・学級・タグの照合**を1つにする（`humanKeyMatching.ts` が既にその第一歩）
- **既定値の埋め方**を1つにする（**変換チェーンの「あと」で埋める**という段階58 の知見を全種へ）
- **`createdAt` / `updatedAt` の扱い**を1つ決める（判断待ち）
- **`.grade` に利用者のセクションを足す**（OWNER 裁定・2026-08-23 に**ここへ畳んだ**）。
  `GradeFrozenScore.frozenByUserId` を行のまま書き出すのに成績アーカイブは利用者を
  持たないので、取り込み側は同じ id の利用者が偶然居なければ **null（操作者不明）へ倒す**。
  試験側と違って別人へすり替わりはしないが、**「誰が確定したか」は端末をまたぐと必ず
  消える**。試験アーカイブは 2026-08-23 に「参照される利用者を全員入れる」へ直した
  （`e4f7bf0c`）ので、**統合するときはその形へ寄せる**

### やらないと決めていること

- **`.grade` の名前ベース外部参照は仕様**（試験・小計・領域はアーカイブに含めず lookup のみ）。統合しても、この非対称は残す
- **資料の評語（`CourseworkLetterScale`）と成績の評定は別概念**（前者は配点表で、同じ `S` が項目ごとに 6/12/18/30 点）。同じ語彙へ寄せない

### 先に片付けておくべきこと

**統合の前に、種ごとの往復が全部緑であること。** 2026-08-22 時点で `.coursework` / `.grade` / `.asb` / `.students` は新規取り込みで損失ゼロ、試験（`.score`）は4件を直した。**この状態を回帰テストで固定してから統合に入る**（`__tests__/import-export/scenarios/roundTripFieldFidelity.test.ts` が試験のぶん）。

---

## 将来の検討 — 畳みを取り消せるようにする（**いまは実装しない**・OWNER 判断 2026-08-22）

`_id_merge` は「**何が何になったか**」しか持たない。だから通知を見ても**元に戻せない**。

**畳む前の姿**（消える敗者の行の全列と、付け替える子の付け替え前の姿）を残せば戻せる。
SQLite の Session 拡張には `sqlite3changeset_invert()` があり、changeset を反転して適用
できる（**`better-sqlite3` から使えるかは未確認**）。使えないなら自前で JSON に残す。

**分散ならではの落とし穴**: 戻しても**次の同期でまた畳まれる**。相手には `mergedInto` の
墓標が残っているため。だから操作は「取り消し」ではなく **「2つに戻して、片方の名前を変える」**
になる。名前を変えれば unique がぶつからない。**利用者がやりたいことでもある。**

**期限は切ってよい。** ただし `_changelog` の理屈（7日で捨てる。捨ててもフルマージで
正しくなる）は**ここには使えない**。

|                                 | 期限           | 捨てたらどうなるか               |
| ------------------------------- | -------------- | -------------------------------- |
| `_changelog`                    | **7日**        | フルマージで正しくなる           |
| `_id_merge`（何が何になったか） | **無し**       | 子の付け替えに要る。捨てられない |
| **元の姿**（戻すため）          | **切ってよい** | **戻せなくなるだけ**             |

**畳みが頻繁に起きるなら、それ自体が設計の失敗**（unique を置くべきでない表に置いている）
なので、記録が膨らむこと自体が警告になる。

---

---

## 判断待ち

**どれも急ぎではない。** 決まればすぐ着手できるものと、調べてから決めるものが混ざっている。
決着したぶんはここから消してある（2026-09-09 に3件を落とした ──
`DELETE_MANUSCRIPT_PAPER` は撤去済み、ライブラリの `folded` 早期 return と
`applyInsert` の UNIQUE 非対称は**どちらもソースで塞がっているのを確認した**）。

| #   | 判断すること                                                             | いまの状態                                                                                                                                                                                                                                                                  | 私の見立て                                                                                                                                                  |
| --- | ------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4   | **`FilterOption` を畳むか**（段階19）                                    | 学級とタグの両方に使う UI 側の選択肢契約。`Classroom` の射影の手写しではない                                                                                                                                                                                                | `Classroom \| Tag` にするとより濁る。**残す**が見立て                                                                                                       |
| 5   | **`ClassroomRosterEntry` を畳むか**（段階19）                            | `ExamClassroom` / `GradeClassroom` / `CourseworkClassroom` の3つを1つの共通UIへ差し込む正規化型                                                                                                                                                                             | `RosterRow` と同じ位置づけ。`studentCount` は既に main で数えていない                                                                                       |
| 7   | **リゾルバ用の取得の `include` を削るか**（性能・**未実測**・R10 指摘7） | `getQuestionScoresForExam` は `examStudent{student}` / `cropRegion{examPage}` / `user` の4段を含む行を数万件返しうる。300名×40設問×2採点者 = 24,000行なら1行あたり5オブジェクト前後を丸ごと複製する。Excel出力・PDF出力・返却スナップショット・裁定サマリの各呼び出しで毎回 | **変換が要るのは `partialScore` 1列だけ**。木ごと複製せずその列だけ倒す手もある。まず実測する。`include` を削るなら OWNER 判断                              |
| 8   | **`id` を持たない手写しの引数型**（段階19 の検査の死角）                 | 20件超。ただし**部分的な payload は設計どおり**（触った列だけを運ぶ）なので、列の数は問題ではない                                                                                                                                                                           | 段階34 で「optional と死んだ列」を潰せば実害は消える。検査を広げるかは、それから                                                                            |
| 10  | **畳みの監査ログを端末をまたいで1行に寄せるか**（段階57 で判明）         | `coalesceKey` では**寄せられない**（書く瞬間にローカルへ在る行しか探さないので、各端末が互いを知らないまま挿入する）。いまは N 端末で N 行残る。寄せるなら決定論的な `AuditLog.id` を `(tableName, losingId, winningId)` から作り、同期の LWW に畳ませる                    | **寄せる**が見立て。合成キーからの決定論 id はこのリポジトリの既定（`ExamSubtotalGroup`・観点間制約）。ただし `recordAuditLog` に `id` の口が無く、設計変更 |
| 12  | **書き出しの入口を1つにまとめるか**（段階68 から切り出した）             | 5つのアーカイブ（`.score` / `.coursework` / `.grade` / `.asb` / `.students`）の書き出しが、画面ごとに別々の入口を持つ。**形式の統合（段階60）とは別で、入口だけなら先に1つにできる**                                                                                        | **まとめる**が見立て。ただし段階60 で形式を1本化するなら、そのとき一緒にやる方が二度手間にならない                                                          |

---

## 関連

| 文書                                                                                 | 関係                                                     |
| ------------------------------------------------------------------------------------ | -------------------------------------------------------- |
| [coding-style.md](./coding-style.md)                                                 | 規約の本文。ここに書いた判断の多くは最終的にあちらへ移る |
| [sync-secondary-unique-hazard.md](./sync-secondary-unique-hazard.md)                 | 段階20 の裏付け（読んで走らせた実測）                    |
| [import-export-architecture.md](./import-export-architecture.md)                     | 段階60 の対象（アーカイブ5種）の構造                     |
| [scoring-scope-and-permissions-design.md](./scoring-scope-and-permissions-design.md) | 採点範囲と権限。#1071                                    |
| [ownership-and-sharing-design.md](./ownership-and-sharing-design.md)                 | 所有と共有。#1127                                        |
| [audit-log-redesign.md](./audit-log-redesign.md)                                     | 監査ログの拡充。#1102                                    |
| [renderer-side-calculation-plan.md](./renderer-side-calculation-plan.md)             | main 側の計算専用 IPC 撤去の残り                         |
| [test-suite-audit.md](./test-suite-audit.md)                                         | 本番を守っていないテストの全数調査                       |
| [type-assertion-audit.md](./type-assertion-audit.md)                                 | `as` の全数調査と処方                                    |
| [schema-relation-audit.md](./schema-relation-audit.md)                               | スキーマのリレーション監査                               |
| [internationalization-plan.md](./internationalization-plan.md)                       | 国際化。**段階表に載せない一計画**（着手時期未定）       |
| [frame-detection-analysis.md](./frame-detection-analysis.md)                         | 採点枠自動検出の分析。§7 に未着手の改善候補3件           |

---

## 解答用紙作成（ASB）の IPC 分割から残ったもの

**分割そのものは済んでいる**（書き込みは実体 × 操作で割れ、原稿用紙はテーブルへ出た）。
計画の文書は役目を終えたので消したが、**片付いていない2点だけここへ移す。**

|                                      |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **既存行のバックフィルをしていない** | `sqlite-nas-sync` の `setupChangelog` はトリガーを張るだけで既存行を流さない。フルマージは `hasChangelogGap` のときだけで、`lastSeenId === 0` では `false` を返す。いま実害が小さいのは「開くだけで保存が走る」ために既存の定義が結果的に流れているからで、**それを止めるとこの偶然の伝搬経路も消える**。対策は同期設定の初期化後に一度だけ `UPDATE Asb... SET id = id` を流すこと（生 SQL なので Prisma の `@updatedAt` は動かず、UPDATE トリガーだけ発火する）。**着手は所有者システム（#1127）の後でよい** — それまで相手端末に届いても一覧に出ない |
| **保存のたびに所有者が保存者へ移る** | `asbDefinition.ts` が更新時にも `userId` を書いている。共有すると「他の教員が開いただけで所有者が移る」ことになる。行き先は決まっていて（[ownership-and-sharing-design.md](./ownership-and-sharing-design.md) §4.4）、`userId` は列名も `onDelete: Cascade` も変えず**作成者の記録として残す**。ロール判定はメンバー表が持つ。**書き換えをやめるのはメンバー表が入った時点**                                                                                                                                                                           |

---

## 段階番号の索引（段階0〜70・すべて済）

**コードとテストのコメントに番号だけが残っているので、何を指すかをここに残す。**
詳しい経緯は issue（#1167〜#1242）とコミットにある。**この表に書き足さない** —
これから起こす段階は上の本文へ書き、済んだらこの表へ1行だけ足す。

| #   | 何をしたか                                                 | コミット                 |
| --- | ---------------------------------------------------------- | ------------------------ |
| 0   | 型だけを通す境界を lint で固定した                         | —                        |
| 1   | `Serialized` / `convert` にバイナリ素通しを追加            | —                        |
| 2   | IPC 境界の統合（エンベロープ）                             | —                        |
| 3   | preload の `invoke`                                        | —                        |
| 4   | ドメイン単位の移行（本体）                                 | —                        |
| 5   | registry 化と手書き契約の削除                              | —                        |
| 6   | lint の締め                                                | —                        |
| 7   | R1 で出た回帰の修正                                        | —                        |
| 8   | R2 で出た回帰の修正                                        | —                        |
| 9   | R3 のレビューと前提の洗い直し                              | —                        |
| 10  | 読み書きを `src/queries/` へ集める形を確立                 | —                        |
| 11  | 楽観更新の撤去とジェスチャの規約                           | —                        |
| 12  | 採点(07)と出力(08)を移す                                   | —                        |
| 13  | 07 の採点行を採点領域の木から取る                          | —                        |
| 14  | `window.electronAPI` を `src/queries/` だけにする          | —                        |
| 15  | ASB: main を実体ごとに分解し、バルクを差分適用に           | —                        |
| 16  | ASB: 型と action を id 基準にする                          | —                        |
| 17  | ASB: IPC を割り、書き込みの関所を置く                      | —                        |
| 18  | 設定の JSON を行へ割る                                     | —                        |
| 19  | DB 行の手写し型を是正し、検査を入れる                      | `87edd03d`               |
| 20  | 競合で負けた行の子を、ライブラリ側で引き取る               | 別リポジトリ             |
| 21  | 採点行は、注釈を書くときに main が用意する                 | `e4962b99`               |
| 22  | FK の無い行参照に FK を張る                                | `94be26fe`               |
| 23  | 小計グループの更新を差分書き込みへ直す                     | `ed59222f`               |
| 24  | 原稿用紙をテーブルへ出し、枝問へも生やす                   | `359cfed9`               |
| 25  | 「LWW が畳む」の説明文に条件を足す                         | `cc496b97`               |
| 26  | 消す前に数え直して中止する（全ての削除）                   | `16f2e139`               |
| 27  | 作成からタグ付けを外す                                     | `a7e06486`               |
| 28  | 旧採点判定への耐性を畳み、7値へ統一する                    | `44cf61c2`               |
| 29  | unique の言い方を「uuid 以外」へそろえる                   | `db3bdde6`               |
| 30  | uuid の複合 unique を張り、回避策を畳む                    | `32822397`               |
| 31  | （欠番）                                                   | —                        |
| 32  | 衝突の検出をエラー文字列から `PRAGMA` へ                   | 別リポジトリ・`97f1def5` |
| 33  | `@unique` を1つずつ判定し、畳めないものを外す              | `d6dcc649` `32822397`    |
| 34  | 書き込みの引数から optional と死んだ列を無くす             | `ee432787`               |
| 35  | 依存を sqlite-nas-sync 0.16.0 へ上げる                     | `7f2abf90`               |
| 36  | 並べ替えの D&D から楽観更新を撤去する                      | `a1a34771`               |
| 37  | 採点の理由を書き残せるようにする                           | `893dd4a6`               |
| 38  | 原稿用紙の upsert が renderer の id を捨てるのを直す       | `de5e9ba5`               |
| 39  | migration をまたいで FK の設定が漏れるのを止める           | `184f4e47`               |
| 40  | 中止したあとの本文を、数え直した件数へそろえる             | `2d3ec5be`               |
| 41  | `guidePosition` を素の `as` から境界コンバータへ           | `acc83d9d`               |
| 42  | 規約テストの走査から漏れている7ファイルを拾う              | `46b31e16`               |
| 43  | 枝問の原稿用紙の幅を通し、超過を入力で止める               | `e0b9c002`               |
| 44  | （未着手。本文を参照）                                     | —                        |
| 45  | 原稿用紙の書き込みを意図ごとに割り、届かない削除を落とす   | `de5e9ba5`               |
| 46  | 選んだ学級が在籍フィルタで消えても見えるようにする         | `edfd5c4f`               |
| 47  | 原稿用紙の既定を用紙設定から決める                         | `9503cbb3`               |
| 48  | 絞り込みから外れた選択済みの生徒を下段に出す               | `217a73b7`               |
| 49  | 設定に残っていた楽観更新を撤去する                         | `bdafe223`               |
| 50  | 生徒の在籍スイッチでも、選んだ生徒を出し続ける             | `e1194dfc`               |
| 51  | localStorage を読む effect を共通の購読へ寄せる            | `cfa694df`               |
| 52  | 渡す人が居ない設定を落とす（`showClassroomReorder`）       | `d871c8b9`               |
| 53  | 関門が保証している型を、そのまま配る                       | `8dcaea36`               |
| 54  | 注釈まわりの `currentUserId` の optional を締める          | `bbcb9353`               |
| 55  | 利用者の設定を localStorage から DB へ寄せる               | `43e9b3dd`               |
| 56  | 届いた更新がユニークに当たっても同期が止まらないようにする | 別リポジトリ・`e99c6a2b` |
| 57  | 畳んだことを黙らない（通知と監査ログ）                     | `144c59fb`               |
| 58  | アーカイブの往復で黙って消える3件を止める                  | `b848cf3b`               |
| 59  | （未着手。本文を参照）                                     | —                        |
| 60  | （未着手。本文を参照）                                     | —                        |
| 61  | `_tombstone` の時刻をミリ秒にする                          | 別リポジトリ・0.19.0     |
| 62  | 取り込みを「上書き / 統合 / 別で追加」の1つの規則へ畳む    | `80048d88`               |
| 63  | 答案の入れ替えが同期を越えられないのを直す                 | `39737851`               |
| 64  | 4つのトップページの一覧UIを1つに揃える                     | `8614f7f8`               |
| 65  | 詳細のパンくずをタブへ、上段をツールバーと中央の題へ       | `c9cb5f54` ほか          |
| 66  | 概要ページ4つを揃え、その場で編集できるようにする          | `a7e06486`               |
| 67  | 試験に「8. 採点確定」の段を足し、8.結果を9.結果へ          | —                        |
| 68  | R12 の指摘9件を直す                                        | `1ea6aed6` ほか          |
| 69  | 監査ログを独立したページへ出し、ページ送りを付ける         | `d49182b7`               |
| 70  | 採点の取り直しを、書いた1行が載っているキーまで狭める      | —                        |
