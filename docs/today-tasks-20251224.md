# 2024年12月24日 タスク整理

## 本日完了したタスク

### 1. Shift+D設問移動時の選択問題 (PR #174)
- 設問移動時にフィルター後の最初の生徒答案が選択されない問題を修正
- タイミング問題（レンダリングサイクル間の状態更新）を解決

### 2. 採点履歴の設問跨ぎ問題 (PR #176)
- `recentlyScoredAnswers`を設問ごとのMapに変更
- 設問変更時に前の設問の採点履歴が残る問題を解決

### 3. 個別表示モードの生徒選択問題 (PR #178)
- 生徒答案選択パネルで「採点データを選択してください」と表示される問題を修正
- 設問切替時に正しいページのpageImageを選択するように修正

### 4. 個別採点モードの「採点時の動作」設定 (PR #180)
- `scoringBehavior`設定が採点後に反映されるように修正
- 次の生徒の同じ設問 / 同じ生徒の次の設問が正しく動作

### 5. 個別表示での採点記号表示（PoC）
- `useImageCanvas`フックに採点記号画像のプリロードを追加
- 設問枠の中央に採点記号を表示する機能を実装

---

## 本日やること

### 採点記号表示の動作確認
- [x] 個別表示で採点済み答案の記号が表示されることを確認
- [x] 各ステータス（正解、不正解、部分点、保留）の記号が正しく表示されるか確認

### 個別表示機能の改善

#### 1. ツールパレットの自動フェードアウト機能
- [ ] 一定時間無操作でツールパレットをフェードアウト
- [ ] マウス操作で再表示
- [ ] フェードイン/アウトアニメーション付き

#### 2. 設問表示の余白計算調整
- [ ] 設問表示拡大時の余白を調整

#### 3. 設問移動時の中央寄せ表示
- [ ] 設問移動時に選択設問を画面内で中央に表示
- [ ] 画面からはみ出ない範囲での中央寄せロジック
- [ ] 全体表示・設問表示・それ以外で統一的に動作

#### 4. 全体表示・設問表示のショートカット追加
- [ ] 現在のショートカット管理システムに統合
- [ ] 設定画面でキー割り当て可能に
- [ ] 一覧表示モードでは無効化

#### 5. アノテーション機能の修正
- [ ] 現状調査（何が動作し、何が動作しないか）
- [ ] 問題点の修正

---

## 今日やらないこと（将来のタスク）

### 採点記号の配置設定機能

#### 短期（次回以降）
- [ ] 位置選択UI（9箇所: 上左、上中央、上右、中左、中央、中右、下左、下中央、下右）
- [ ] サイズ調整機能
- [ ] 表示ON/OFF切り替え

#### 中期（DB設計変更が必要）
- [ ] プロジェクト単位での採点記号設定保存
- [ ] 設問ごとの位置カスタマイズ
- [ ] ユーザーごとのデフォルト設定

#### 長期（アーキテクチャ検討が必要）
- [ ] 他プロジェクトの設定を継承/コピー
- [ ] 08-export（結果出力）との設定統合
- [ ] PDF出力とCanvas表示の設定共有

### その他の保留タスク
- [ ] 個人成績表PDF出力機能
- [ ] 画像前処理機能の追加
- [ ] 大規模ファイルの分割（02-template, 03-region-info等）

---

## 技術メモ

### 採点記号表示の実装状況
- 画像キャッシュ: `/public/score-assets/` から読み込み、refでキャッシュ
- 位置計算: 設問領域の中央に配置
- サイズ: 設問領域高さの50%、最大100px

### 将来の設定保存に必要なDB変更案
```
// 案1: Projectテーブルに追加
Project {
  scoringMarkConfig: Json?  // 採点記号設定
}

// 案2: 新規テーブル
ScoringMarkConfig {
  id: String
  projectId: String?
  cropRegionId: String?  // 設問別設定の場合
  userId: String?        // ユーザー別デフォルトの場合
  position: String       // "top-left", "center", etc.
  size: Int
  showMark: Boolean
  showScore: Boolean
}
```

### PDF出力との統合ポイント
- `pdfExport.ts`の`calculateMarkPosition()`関数を共有可能
- 設定UIを共通化して両方に適用
