# Score at Once データベース設定
# 
# 共有ドライブで使用する場合は、以下のようにアプリケーションと同階層の
# dataフォルダ内にデータベースファイルが配置されます
#
# 例: /shared-drive/score-at-once-electron/data/database.db
#
# 本番環境では自動的に設定されるため、このファイルは主に開発用です

DATABASE_URL="file:./data/database.db"