了解です。以下の観点で挙動を整理したいので、それぞれ「こう動いてほしい」と
いう期待を教えてください。eslint の依存配列推奨に従いつつ、副作用を分割・統
合する前提で考えています。

- 一覧表示：設問変更時
  - フィルター一致の答案がある場合：最初の生徒答案を選択する。
  - フィルター一致がゼロの場合：選択を解除する（＝どの答案も選ばない）。
  - この際、模範解答に自動フォーカスするケースはありますか？
- 一覧表示：フィルター切り替え時
  - 直前に選んでいた答案がフィルターから外れたとき（例：正答フィルターON
    で誤答答案を選択中 → フィルター適用）どうするべきでしょう？
  - 選択が外れた後は「初めの候補に自動移動」「クリア」「直前の選択を保
    持」など、どれが望ましいですか？
- 一覧表示：採点ステータス変更時
  - 採点によって答案が別ステータスに移動しフィルターからいなくなった場合
    の選択処理はどうしますか？
- 個別表示：設問変更時
  - 現在の生徒答案をそのまま保持する前提で良いか？
  - もしその設問に対応する採点領域が存在しない答案を選んでいた場合はどう
    しましょう？
- モード切替（一覧 ↔ 個別）
  - 一覧 → 個別：複数選択があれば先頭だけに絞る、などルールが既にあります
    が、設問選択やフィルター状態をどう扱うべきかご意見ください。
  - 個別 → 一覧：前回の一覧選択を復元するのか、個別で見ていた答案を使い続
    けるのか。
- 初期ロード／イニシャライズ
  - 初回ロード時（設問も生徒も未選択）、どの順番で設定を決めるべきでしょ
    う？ 例：最初に設問→次に最初の表示対象答案を自動選択、など。

これらのユースケース別に望む挙動をお伝えいただければ、依存配列を正しく揃え
つつ、副作用を安全に組み直す方針をご提案できます。

1. 模範解答は採点しないため、いかなる場合でも模範解答にフォーカスがあたるのは不適切です。
2. フィルターから外れた場合、フィルターに合致する生徒答案に自動移動すべきです。
   表示されていない答案が選択されているのは不適切です。
   また、選択されないのは、フィルターに合致する答案が一つもないときに限るのが望ましいです。
   一方で、フィルターの変更のみにおいて、生徒答案がそのまま表示されている場合は、このときのみ当該生徒答案の選択を継続すべきです。
3. 採点ステータスを変更したとき、個別表示にするときは、選択されている設問の選択されている生徒答案を表示すべきです。
   また、一覧表示にするときは、選択されている設問のフィルターに合致する最初の答案を選択すべきです。
4. 一覧表示において採点を行い、フィルターからいなくなった場合も、設問の移動やRによるリロードをしない限り、表示し続けるべきです。
   これは、採点が正しく行われ採点ステータスが変更したことをユーザーが確認したり、採点が誤った場合に直ちに採点ステータスを再変更できるようにするべきだからです。
   このため、フィルターとは別に表示している答案のlistをstateにもつ、何層にもわたる複雑な状態管理をすでに実装しているはずです。
5. 個別表示においては、設問変更時、選択されている生徒答案を維持します。
   また、採点領域は生徒答案ごとではなく、プロジェクトごとに存在するため、（答案画像が存在しないことはあれど）採点領域が存在しないということはありません。
6. 個別変更時、複数答案を選択していた場合は、先頭に絞ってください。
   個別から一覧の場合は、設問だけ保持し、生徒選択は、フィルターの先頭・フィルター対象がなければ選択を外すロジックに従ってください。
7. 初回ロードというのが7.一括採点のページを開いたときを指すのであれば、最初の設問を選択し、フィルター対象のはじめの答案、なければ無選択のロジックに従います。

わからないことがあれば、さらに追加で質問してください。
