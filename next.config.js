/**
 * @type {import('next').NextConfig}
 */
const nextConfig = {
  // eslint: ["pages"],
  // output: "export",
  images: {
    domains: ["localhost"], // 画像を置いているドメイン
  },
}

module.exports = nextConfig
