import React from "react"
import Layout from "../components/Layout"

const Page = (): JSX.Element => (
  <Layout title="About | Next.js + TypeScript + Electron Example">
    This is the allocation page
  </Layout>
)

export default Page
