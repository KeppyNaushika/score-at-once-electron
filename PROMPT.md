# 一括採点 - 設計仕様書

## このドキュメントの目的

このドキュメントは、教員と開発者をつなぐ架け橋です。教育の専門家でない開発者が読んでも「何を実現したいのか」がわかり、教員が読んでも「このソフトウェアで何ができるのか」がわかるように書かれています。

---

## アプリケーションの目的

試験の答案画像をデジタル採点し、結果をExcelファイル・PDFファイルとして出力するデスクトップアプリケーションです。

**特に重要な特徴**:
- 複数の教員が同時に採点作業を行える
- 採点結果を後から統合できる
- キーボード操作を最優先とした効率的な採点

---

## 技術スタック

| 役割 | 技術 | 選定理由 |
|------|------|----------|
| デスクトップアプリ | Electron | Windows/Mac/Linux対応、ファイルシステムアクセス |
| フロントエンド | React + TypeScript | 型安全性、コンポーネント再利用性 |
| スタイリング | Tailwind CSS | 高速なUI開発 |
| データベース | Prisma + SQLite | 共有フォルダに配置可能、ORMによる型安全 |
| 画像処理 | sharp | 高速な画像変換 |
| PDF読み込み | PDF.js | PDFをページ単位でPNG画像に変換 |
| Excel出力 | exceljs | 関数付きExcelファイル生成 |
| PDF出力 | pdf-lib | 採点済み答案PDFの生成 |

---

## 採点ワークフロー

### 8段階の作業フロー

1. **模範解答アップロード**: 解答用紙のPDF/画像を登録
2. **採点領域作成**: 設問ごとの領域をドラッグで定義
3. **領域情報設定**: 配点・ラベルを設定
4. **小計点の設定**: 大問ごと・観点別などの集計グループを設定
5. **受験生徒管理**: 採点対象の生徒を登録
6. **答案アップロード**: スキャンした答案画像を読み込み・生徒に紐付け
7. **採点実行**: キーボード操作で効率的に採点
8. **結果出力**: PDF・Excel・個人成績表を出力

---

## 採点システム

### デュアルモード採点

**個別採点モード**（記述式・作文問題向け）
- 1つの答案を大きく表示
- ズーム・パン操作で細部を確認
- 部分点・コメント入力に最適

**一覧採点モード**（客観式・短答式問題向け）
- 複数の答案をグリッド表示
- 同じ設問を一覧で比較
- 高速な一括採点に最適

### 採点の種類

| 採点 | 説明 |
|------|------|
| 正答 | 完全正解 |
| 部分点 | 一部正解（点数入力可能） |
| 誤答 | 不正解 |
| 無答 | 未記入 |
| 保留 | 後で再確認が必要 |
| 未採点 | まだ採点していない |

---

## キーボードショートカット

すべてのショートカットは設定画面からカスタマイズ可能です。

### 採点操作

| キー | 機能 |
|------|------|
| Q | 未採点に戻す |
| E | 正答 |
| F | 部分点（モーダルが開く） |
| J | 保留 |
| O | 誤答 |
| P | 無答 |
| 0-9, . | 部分点入力（モーダルを開いて入力） |

### ナビゲーション

| キー | 機能 |
|------|------|
| W/A/S/D | グリッド内移動（上/左/下/右） |
| 矢印キー | 答案間移動 |
| Shift+A/D | 設問切り替え |
| =/- | ズームイン/アウト |
| 0 | ズームリセット |

### フィルタ

| キー | 機能 |
|------|------|
| Alt+Q | 未採点フィルタ切替 |
| Alt+E | 正答フィルタ切替 |
| Alt+F | 部分点フィルタ切替 |
| Alt+J | 保留フィルタ切替 |
| Alt+O | 誤答フィルタ切替 |
| Alt+P | 無答フィルタ切替 |
| R | フィルタ更新 |

### 表示

| キー | 機能 |
|------|------|
| N | 生徒名表示/非表示切替 |
| A | 答案全体表示（個別モード） |
| C | 設問領域表示（個別モード） |

### 描画ツール

| キー | 機能 |
|------|------|
| H | ハンドツール（パン操作） |
| G | 選択ツール |
| T | テキストツール |
| L | 線ツール |
| B | 矩形ツール |
| Y | 楕円ツール |

---

## 結果出力

### PDF出力

採点記号を印字した答案PDFを出力します。

**印字位置の設定**: 各設問領域の9点（四隅、各辺中央、中心）を基準に配置可能

**印字内容の選択**:
- 採点記号（○×など）
- 点数
- 両方

**採点種類ごとの設定**: 正答・誤答・部分点など、種類ごとに表示/非表示を切替可能

**保留警告**: 保留状態の答案がある場合、出力前に警告を表示

### Excel出力

採点結果をExcelファイルとして出力します。

**2シート構成**:
- 点数一覧シート: 各生徒の設問別得点
- 正誤一覧シート: 各生徒の設問別正誤

**組み込み関数**: SUM、COUNTIFS等で合計点・順位を自動計算

**見込受験者**: 統計計算（平均等）からは除外、個人成績のみ記録

### 個人成績表PDF出力

生徒一人につき1枚のPDF成績表を一括出力します。

**含まれる情報**:
- 生徒情報（氏名・学籍番号）
- 設問別得点
- 合計点・順位
- 成績分布グラフ

---

## 受験生徒管理の設計思想

### 基本原則

受験生徒リストは、学級データから完全に独立して管理します。

**なぜ独立が必要か**:
- 試験後に学級の所属情報が変更されても、採点データに影響を与えない
- 過去の試験データの整合性を保証する
- 受験時点での生徒順序を永続的に保持する

### ProjectStudentテーブル

プロジェクト（試験）ごとに受験生徒リストを持ちます。

```
Project ←→ ProjectStudent ←→ Student
  1          多                 1
```

**独立性の保証**:
- 学級所属データ（StudentClassMembership）への参照は表示用のみ
- 生徒の学級が変わっても、ProjectStudentのデータは変わらない
- 採点データは全てProjectStudent経由でStudentに紐付く

### 順序管理

**customOrder**フィールドで表示順序を制御:
- 一括追加時: 学級順→出席番号順で自動設定
- 後から変更: ドラッグ&ドロップで順序変更可能
- この順序が採点画面の表示順・書き出し順に使用される

### 学級情報の活用

**表示・フィルタリング目的のみ**:
- 学級フィルタは「この学級に所属したことがある生徒」で絞り込み
- 過去の所属履歴も含めてマッチング
- フィルタ結果はProjectStudentのデータを変更しない

---

## 答案入れ替えシステム

配置済み答案の管理機能です。

### 楽観的更新

答案の入れ替え操作は即座に画面に反映されます（赤いオーバーレイで変更箇所を強調）。
実際のデータベース反映は「変更を反映」ボタンで明示的に実行します。

### 3つの処理オプション

| オプション | 説明 |
|------------|------|
| 採点情報も一緒に入れ替え（推奨） | 答案画像と採点結果の論理的一致を保持 |
| 答案画像のみ入れ替え（注意） | 採点データは元位置に残る。データ不整合リスクあり |
| キャンセル | 変更を破棄して元の状態に戻す |

---

## 複数教員による協調採点

### 設計思想

同じ試験を複数の教員が分担して採点できます。

**採点時**: 各教員の採点結果は独立して記録（scoredByUserIdで識別）
**統合時**: 同じ答案への複数採点を比較し、最終決定

### 楽観的ロック

複数教員が同時に同じ答案を編集した場合の競合制御:
- 更新時にバージョンチェック
- 競合検出時は警告を表示し、最新データの再読み込みを促す

---

## 型管理の方針

### 基本原則

1. **Prisma型を最優先**: データベースモデルはPrisma生成型をそのまま使用
2. **anyとasは原則禁止**: 型安全性を損なう記述は避ける

### 型定義の配置ルール

**単一ファイルで使用する型**:
- そのファイル内で宣言
- 例: hooksの引数・返り値の型、コンポーネントのProps

**複数ファイルで使用する型**:
- 上位ディレクトリのtypes.tsに配置
- 例: 機能ディレクトリ内の共通型

**アプリケーション全体で使用する型**:
- /types/ディレクトリに配置
- 大規模で主要な機能の型はここに置くと全体像が把握しやすい

### Prisma型の拡張

IPCでの受け渡し時にDecimal→number変換が必要な場合など、Prisma型の拡張は`types/prisma-extensions.ts`に集約します。

---

## 未実装機能

### 画像前処理（将来実装予定）

- 傾き補正
- 台形補正
- 二値化処理

OpenCV.jsを使用予定。現在は手動での調整を前提としています。

### 解答枠自動認識（将来実装予定）

模範解答画像から解答枠を自動検出する機能。
あくまで補助機能として提供予定（精度は画像状態に依存）。

---

## 技術的課題

### NAS対応のSQLite

**課題**: SQLiteは共有ドライブ（NAS）での使用に制限があります。

**現状**: ローカルでの使用を前提
**将来**: NAS対応のnpmパッケージを開発中。対応完了後にアップデート予定。

### Project-Classリレーション

**課題**: 学級平均点などの集計のため、Project-Class間の直接リレーションが必要になる可能性があります。

**現状**: StudentClassMembership経由での間接参照のみ
**検討中**: 必要に応じてスキーマ拡張を検討

---

## 領域タイプ

採点領域には以下のタイプがあります。

| タイプ | 用途 |
|--------|------|
| QUESTION_ANSWER | 設問の解答欄（採点対象） |
| STUDENT_NAME | 氏名欄 |
| STUDENT_ID | 学籍番号欄 |
| TOTAL_SCORE | 合計点表示欄 |
| SUBTOTAL_SCORE | 小計点表示欄 |
| MARK | マーク欄 |
| COMMENT | コメント欄 |
| OTHER | その他 |

---

## 用語集

| 用語 | 説明 |
|------|------|
| プロジェクト | 1つの試験。模範解答・受験生徒・採点結果をまとめて管理 |
| ProjectStudent | プロジェクトと生徒の紐付け。受験状態・表示順序を管理 |
| CropRegion | 採点領域。設問や氏名欄などの位置・サイズを定義 |
| QuestionScore | 1つの設問に対する1人の生徒の採点結果 |
| PageImage | 答案画像。模範解答(MASTER_ANSWER)または生徒答案(STUDENT_ANSWER) |
